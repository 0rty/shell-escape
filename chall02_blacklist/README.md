# Challenge 02 - Blacklist Bypass 
In this lab, some commands are blacklisted (cat, ls, whoami ...).
The goal is to bypass the restriction to get the flag even if it's forbidden.
Remember that few characters are ignored by the shell

## Flag format

`HackUTT{...}`
